import * as React from 'react';

interface MaterialIconProps {
    icon: string;
    className?: string;
    hasRipple?: boolean;
}

declare class MaterialIcon extends React.Component<MaterialIconProps, any> {
}

export default MaterialIcon;
